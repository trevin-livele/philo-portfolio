import React from "react";
import { Link } from "react-router-dom";
import { createPageUrl } from "@/utils";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Badge } from "@/components/ui/badge";
import { ArrowRight, BarChart3, Brain, FileText, TrendingUp, Target, Users, Globe } from "lucide-react";

import HeroSection from "../components/home/HeroSection";
import StatsSection from "../components/home/StatsSection";
import ExpertiseSection from "../components/home/ExpertiseSection";
import FeaturedWork from "../components/home/FeaturedWork";

export default function Home() {
  return (
    <div className="min-h-screen">
      <HeroSection />
      <StatsSection />
      <ExpertiseSection />
      <FeaturedWork />
      
      {/* Call to Action Section */}
      <section className="py-20 bg-gradient-to-r from-slate-900 to-emerald-900">
        <div className="max-w-4xl mx-auto text-center px-6">
          <h2 className="text-3xl md:text-4xl font-bold text-white mb-6">
            Ready to Transform Your M&E Approach?
          </h2>
          <p className="text-xl text-slate-300 mb-8 max-w-2xl mx-auto">
            Let's discuss how data-driven monitoring and evaluation can amplify your program's impact.
          </p>
          <div className="flex flex-col sm:flex-row gap-4 justify-center">
            <Link to={createPageUrl("About")}>
              <Button size="lg" className="bg-white text-slate-900 hover:bg-slate-100 font-semibold">
                Learn More About Me
                <ArrowRight className="w-5 h-5 ml-2" />
              </Button>
            </Link>
            <Button size="lg" variant="outline" className="border-white text-white hover:bg-white hover:text-slate-900 font-semibold">
              View My Work
            </Button>
          </div>
        </div>
      </section>
    </div>
  );
}